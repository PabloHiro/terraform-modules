name: Test
on: [push]
jobs:
  terraform-qa:
    runs-on: docker pull mijdavis2/pre-commit-terraform
    steps:
    - uses: actions/checkout@v3
    - name: Call Composite Action terraform-qa
      id: terraform-qa
      uses: actions/terraform-qa@main